package com.revobank.backend.fitbank.repository;

import com.revobank.backend.fitbank.model.db.LogErrorModel;
import org.springframework.data.repository.CrudRepository;

public interface LogErrorModelRepository extends CrudRepository<LogErrorModel, Long> {
}
